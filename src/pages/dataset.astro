---
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "Benchmark Dataset Information"; // Will be translated by Header
const activeNav = "dataset";
---
<BaseLayout title={pageTitle} activeNav={activeNav}>
    <main class="container mx-auto px-4 py-8 space-y-12">
        <section id="dataset-description" class="prose dark:prose-invert max-w-none">
            <h2 data-lang-key="dataset_desc">Dataset Details</h2>
            <p data-lang-key="dataset_source">Source: WildStory (Versions: English, Chinese)</p>
            <p data-lang-key="dataset_stats">Statistics: [80] stories, Avg. [10-15] shots per story.</p>
            <p>[Placeholder for more detailed description of the dataset, its characteristics, purpose, and any preprocessing steps.]</p>
            <p><strong>Data Prerequisites:</strong></p>
            <ul>
                <li>CRef images per character: 1-10 (style consistent).</li>
                <li>Script length per story: 4-30 shots.</li>
                <li>Max characters per shot: 6.</li>
                <li>Separate evaluation for realistic and comic styles recommended.</li>
            </ul>
        </section>

        <section id="data-structure" class="prose dark:prose-invert max-w-none">
            <h2 data-lang-key="datastructure_title">Data Structure (stories_data)</h2>
            <p>[Placeholder explanation of the main data structure used for stories and characters.]</p>
            <pre class="bg-gray-100 dark:bg-gray-800 p-4 rounded-md overflow-x-auto"><code>{`
stories_data = {
    "story_id (e.g., 01)": {
        "type": "[e.g., WildStory_en]",
        "shots": [
            {
                "shot_id": "00",
                "index": 1,
                "scene": "[Scene description]",
                "plot": "[Plot description]",
                "script": "[Detailed script/prompt for the shot]",
                "camera": "[Camera angle/shot type, e.g., Medium shot]",
                "character_key": ["Litte Brown Rabbit", ...],
                "character_name": ["[Actual name if different]", ...]
            },
            // ... more shots
        ],
        "characters": {
            "character_key (e.g., Litte Brown Rabbit)": {
                "name": "Litte Brown Rabbit",
                "key": "Litte Brown Rabbit",
                "prompt": "[Character description prompt]",
                "tag": "[Any relevant tags]",
                "num_of_appearance": 16,
                "images": [
                    "/path/to/ref/image1.jpg", // Relative path example
                    "/path/to/ref/image2.jpg",
                    // ... more reference images
                ]
            },
            // ... more characters
        }
    },
    // ... more stories
}
            `}</code></pre>
        </section>

        <section id="output-structure" class="prose dark:prose-invert max-w-none">
             <h2 data-lang-key="outputstructure_title">Output Structure (Benchmark Results)</h2>
             <p>[Placeholder explanation of how model outputs are stored.]</p>
            <pre class="bg-gray-100 dark:bg-gray-800 p-4 rounded-md overflow-x-auto"><code>{`
dataset_processed:
    storygen:
        data_set(WildStory_en, WildStory_ch):
            story_id(01):
                shots.json # Potentially processed/evaluated shot data

outputs:
    storygen:
        mode("multi-image-condition", "auto-regressive", "mix"):
            data_set(WildStory_en, WildStory_ch):
                story_id(01):
                    time_stamp(20250430-022517):
                        shot_id.png (e.g., shot_01.png)
                        # Maybe evaluation results per shot/story here too?
            `}</code></pre>
        </section>

        <section id="data-examples" class="prose dark:prose-invert max-w-none">
            <h2 data-lang-key="dataexamples_title">Data Examples</h2>
            <div class="data-example">
                <p><strong>Example Script (Story 01, Shot 00):</strong> [Placeholder script text...]</p>
                <p><strong>Example Character (Little Brown Rabbit):</strong>
                    <img src="/img/placeholder.png" alt="Ref 1" class="max-w-[150px] m-1 align-middle inline-block border border-gray-300 dark:border-gray-600">
                    <img src="/img/placeholder.png" alt="Ref 2" class="max-w-[150px] m-1 align-middle inline-block border border-gray-300 dark:border-gray-600">
                </p>
            </div>
            {/* <!-- Add more examples if needed --> */}
        </section>
    </main>
</BaseLayout> 